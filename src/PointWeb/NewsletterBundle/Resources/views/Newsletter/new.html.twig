{% extends 'PointWebAdminBundle::base.html.twig' %}

{% block content_right %}
    <h1>Newsletter</h1>

    {{ form_start(form, {'attr': {'class':'form-horizontal' }}) }}
    <fieldset>
        <div class="form-group">
            {{ form_label(form.object) }}
            <div class="col-lg-10">
                {{ form_errors(form.object) }}
                {{ form_widget(form.object) }}
            </div>
        </div>
        <div class="form-group">
            {{ form_label(form.addresser) }}
            <div class="col-lg-10">
                {{ form_errors(form.addresser) }}
                {{ form_widget(form.addresser) }}
            </div>
        </div>
        <div class="form-group">
            {{ form_label(form.message) }}
            <div class="col-lg-10">
                {{ form_errors(form.message) }}
                {{ form_widget(form.message) }}
            </div>
        </div>
        <div class="form-group">
            {{ form_label(form.newsletterGroup) }}
            <div class="col-lg-10">
                {{ form_errors(form.newsletterGroup) }}
                {{ form_widget(form.newsletterGroup) }}
            </div>
        </div>
    </fieldset>
    {{ form_end(form) }}

    <a href="{{ path('admin_newsletter') }}" class="btn btn-primary"><i class="fa fa-arrow-circle-left"></i> Retour à la liste</a>
{% endblock %}


{% extends 'PointWebAdminBundle::base.html.twig' %}

{% block content_right %}
    <h1>Création d'un abonné</h1>

    {{ form_start(form, {'attr': {'class':'form-horizontal' }}) }}
    <fieldset>
        <div class="form-group">
            {{ form_label(form.email) }}
            <div class="col-lg-10">
                {{ form_errors(form.email) }}
                {{ form_widget(form.email) }}
            </div>
        </div>
        <div class="form-group">
            {{ form_label(form.newsletterGroup) }}
            <div class="col-lg-10">
                {{ form_errors(form.newsletterGroup) }}
                {{ form_widget(form.newsletterGroup) }}
            </div>
        </div>
    </fieldset>
    {{ form_end(form) }}

    <a href="{{ path('admin_newsletter_mail') }}" class="btn btn-primary"><i class="fa fa-arrow-circle-left"></i> Retour à la liste</a>
{% endblock %}


{% extends 'PointWebAdminBundle::base.html.twig' %}

{% block  content_right %}
    <h1>Liste des messages contact Devis</h1>

    <table class="table table-striped table-hover">
        <thead>
        <tr>
            <th>Nom</th>
            <th>Email</th>
            <th style="width: 125px;">Actions</th>
        </tr>
        </thead>
        <tbody>
        {% for entity in entities %}
            <tr>
                <td><a href="{{ path('admin_contact_show', { 'id': entity.id }) }}">{{ entity.firstname }}</a></td>
                <td>{{ entity.email }}</td>
                <td>
                    <ul>
                        <div class="btn-group-vertical">
                            <a href="{{ path('admin_contact_show', {'id':entity.id}) }}" class="btn btn-primary"><i
                                        class="fa fa-eye"></i> Voir</a>
                        </div>
                         <div class="btn-group-vertical">
                            <a href="{{ path('admin_contact_delete', {'id':entity.id}) }}" class="btn btn-danger"><i
                                        class="fa fa-trash"></i> Supprimer</a>
                        </div>
                    </ul>
                </td>
            </tr>
        {% endfor %}
        </tbody>
    </table>
{% endblock %}
